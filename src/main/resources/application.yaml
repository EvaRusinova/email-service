server:
  port: 8082

# Spring
spring:
  profiles:
    active: local
  datasource:
    url: jdbc:h2:mem:testdb
  driver-class-name: org.h2.Driver
  username: sa
  password:
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

# Email
mail:
  host: smtp.gmail.com
  port: 587
  username: user.service.autogenerated@gmail.com
  password: alnstzwgvxyiqtsj
  properties:
    protocol: smtp
    auth: true
    starttls: true
    debug: true

#RabbitMq
rabbitmq:
  host: localhost
  port: 5672
  username: guest
  password: guest
  virtualHost: "/"
  queues:
    user-registration-q: user-registration-queue
    email-verification-q: email-verification-queue
  routing-keys:
    user-registration-rk: user-registration-key
    email-verification-rk: email-verification-key
  exchanges:
    user-registration-ex: user-registration-exchange
    email-verification-ex: email-verification-exchange
